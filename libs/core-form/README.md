# core-form

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-form` to build the library.

## Running unit tests

Run `nx test core-form` to execute the unit tests via [Jest](https://jestjs.io).
